<Page x:Class="HotelMng.Pages.RoomMngPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:HotelMng.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="RoomMngPage">

    <Page.Resources>
        <x:Array Type="sys:String" x:Key="a">
            <sys:String>Tầng 1</sys:String>
            <sys:String>Tầng 1</sys:String>
            <sys:String>Tầng 1</sys:String>
            <sys:String>Tầng 1</sys:String>
        </x:Array>
    </Page.Resources>
    <Grid>
        <GroupBox Header="Phân loại phòng">
            <StackPanel>
                <StackPanel>
                    <TextBlock Text="Theo lầu:"/>
                    <Separator/>
                    <ListView ItemsSource="{StaticResource a}">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"></TextBlock>
                        </DataTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</Page>
